import { SlefskipoptionDirective } from './slefskipoption.directive';

describe('SlefskipoptionDirective', () => {
  it('should create an instance', () => {
    const directive = new SlefskipoptionDirective();
    expect(directive).toBeTruthy();
  });
});
